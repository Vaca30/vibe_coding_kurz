# 4_subagent

Use prompt `Run dead-code-analyzer on project my-ecommerce`

## Setup

Clone the langchain repository into this directory:

```bash
git clone https://github.com/lukaskellerstein/my-ecommerce.git
```

To update it later:

```bash
cd my-ecommerce && git pull