[project]
name = "langchain-basics"
version = "0.1.0"
description = "Minimal LangChain basics lesson"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain",
    "langchain-openai",
    "python-dotenv",
]

[tool.uv]
package = false
