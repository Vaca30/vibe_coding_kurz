[project]
name = "01-ukol-tool-calling"
version = "0.1.0"
description = "LLM tool calling demo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.66.3",
    "python-dotenv>=1.0.1",
]