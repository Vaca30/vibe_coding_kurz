# 4_subagent

Use prompt `Let's analyze dead code in the 'my-ecommerce' project`

## Setup

Clone the langchain repository into this directory:

```bash
git clone https://github.com/lukaskellerstein/my-ecommerce.git
```

To update it later:

```bash
cd my-ecommerce && git pullnn