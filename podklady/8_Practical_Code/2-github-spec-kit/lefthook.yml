pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm biome check --staged --no-errors-on-unmatched
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    unit:
      run: pnpm test:unit
