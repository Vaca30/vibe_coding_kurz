apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openclaw
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`OPENCLAW_INSTANCE-claw.cellarwood.org`)
      kind: Rule
      services:
        - name: openclaw
          port: 18789
