apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openclaw-lukas

resources:
  - ../../base
  # Uncomment to expose this instance publicly via Traefik → cloudflared.
  # Pair with the configmap patch below to switch the gateway bind off loopback.
  # - ingressroute.yaml

# patches:
#   - path: configmap-patch.yaml
#     target:
#       kind: ConfigMap
#       name: openclaw-config

labels:
  - pairs:
      openclaw.instance: lukas
    includeSelectors: true
