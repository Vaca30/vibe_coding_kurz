[project]
name = "langchain-tools-agents"
version = "0.1.0"
description = "LangChain tools and agents lesson"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain",
    "langchain-openai",
    "python-dotenv",
]

[tool.uv]
package = false
